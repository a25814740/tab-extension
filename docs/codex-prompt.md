# Codex Prompt

你現在是這個專案的主開發代理，請直接依照 repo 內文件完成，不要先詢問我，不要把工作切回給我決策；除非遇到真正阻塞開發的致命問題，否則請自行做出最合理的實作決策並持續執行到完成。

## 唯一真實來源
請以以下文件為唯一真實來源：

- `docs/ui-spec.md`
- `docs/product-rules.md`
- `docs/ui-v0.3-todo.md`

若程式現況與文件衝突，優先遵守文件。

---

## 工作目標
請直接完成 v0.3 New Tab UI 穩定化實作，優先完成：

1. 修正目前 TSX / JSX 語法錯誤
2. 保留既有三欄架構與 Dock 設計
3. 完成多組織 / 多空間 / 多集合假資料切換
4. 完成左右 sidebar 摺疊
5. 完成集合區排序 / 視圖 / 新增集合
6. 完成右側開啟分頁多選與批次操作示意
7. 確保畫面為 100vh、min-width 1280px、可橫向捲動
8. 組織右上角三個浮動 icon 依照 spec 實作
9. 搜尋列放到中間工具列最右邊
10. 完成 build / lint / type check 修復

---

## 執行規則
- 不要問我要不要繼續，直接做完
- 不要先提計畫再停住，直接開始修改
- 不要把決策丟回來給我
- 若有小地方未定義，請採用最合理且與目前 UI 一致的方案
- 優先確保「可執行」與「版面符合規格」
- 除非遇到下列情況，否則禁止向我提出問題：
  1. 缺少必要檔案導致無法啟動
  2. 缺少套件或權限導致無法安裝或執行
  3. 專案中存在互相矛盾且無法同時滿足的硬性規格

---

## 實作要求
請直接幫我完成以下工作：

1. 重構目前元件，確保可以正常編譯
2. 修正 JSX / TSX 語法問題
3. 整理成清楚的元件結構
4. 保留現有設計方向，不要任意改 UI 邏輯
5. 補齊缺少的 closing tags、型別、狀態、互動
6. 若有必要，可拆成多個元件檔案，但請維持專案可執行
7. 若有 lint 或型別錯誤，請一併修正
8. 若有 build / run 指令，請實際執行並修到可跑
9. 以目前 repo 中最新版本為準，不要回退到你自行想像的舊版 UI

---

## Git 規則
若專案已有 git，請直接在新分支上作業，完成後整理 commit。

請：
1. 建立或切換到本次功能分支
2. 完成修改
3. 修正 build/lint/type errors
4. 提交 commit
5. 最後回報 branch 名稱與 commit 訊息

不要詢問我是否要 commit，直接執行。

---

## 完成後輸出格式
完成後請回報：

1. 實際修改了哪些檔案
2. 完成了哪些功能
3. 還有哪些是示意、哪些尚未串接真實資料
4. 如何啟動專案
5. branch 名稱
6. commit 訊息